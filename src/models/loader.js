import User from './User.js';
// import OtroModelo from './otromodelo';

export default () => {
	// Ejemplo de cómo hacer las relaciones
	// recuerden considerar bien las llaves foráneas

	// User.hasMany(OtroModelo);
	// OtroModelo.belongsTo(User);
};
